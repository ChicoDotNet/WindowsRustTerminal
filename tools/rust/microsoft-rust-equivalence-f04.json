{
  "schemaVersion": 1,
  "description": "F04 TextAttribute method-level equivalence evidence.",
  "expectedCoveragePriority": 24,
  "expectedCoverage": {
    "textBuffer": { "Exact": 10, "Partial": 4, "Missing": 0 }
  },
  "expectedGlobalCoveragePriority": 24,
  "expectedGlobalCoverage": {
    "Exact": 237,
    "Stronger": 11,
    "Partial": 347,
    "Platform-only": 63,
    "UI-managed": 22,
    "Missing": 418
  },
  "entries": [
    {
      "suite": "textBuffer",
      "source": "TextAttributeTests.cpp",
      "method": "TestRoundtripLegacy",
      "coverage": "Exact",
      "rustWitnesses": ["microsoft_f04_text_attribute_legacy_roundtrip_matches_source_contract"],
      "notes": "Rust reproduces Microsoft's FOREGROUND_BLUE | BACKGROUND_RED construction, IsLegacy observable, and exact legacy round-trip through the production TextAttribute owner."
    },
    {
      "suite": "textBuffer",
      "source": "TextAttributeTests.cpp",
      "method": "TestRoundtripMetaBits",
      "coverage": "Exact",
      "rustWitnesses": ["microsoft_f04_text_attribute_meta_bits_roundtrip_matches_source_contract"],
      "notes": "Rust reproduces the five Microsoft COMMON_LVB meta-bit vectors, preserves each bit in TextAttribute character attributes, and round-trips the complete legacy word."
    },
    {
      "suite": "textBuffer",
      "source": "TextAttributeTests.cpp",
      "method": "TestRoundtripExhaustive",
      "coverage": "Exact",
      "rustWitnesses": ["microsoft_f04_text_attribute_exhaustive_legacy_roundtrip_matches_source_contract"],
      "notes": "Rust iterates the Microsoft legacy attribute domain, skips the same reserved and DBCS bits, and requires exact round-trip through the production TextAttribute owner."
    },
    {
      "suite": "textBuffer",
      "source": "TextAttributeTests.cpp",
      "method": "TestRoundtripDefaultColors",
      "coverage": "Exact",
      "rustWitnesses": ["microsoft_f04_text_attribute_default_colors_roundtrip_matches_source_contract"],
      "notes": "Rust uses an explicit LegacyColorDefaults value instead of Microsoft's mutable process-global defaults, but reproduces all three source observables: default foreground, default background, and both defaults map bidirectionally to the configured legacy indices."
    }
  ]
}
